collectors:

- type: php-composer
  path: /
  actors:
  # pull requests for updates to major versions
  - type: php-composer
    versions: "Y.0.0"
    settings:
      commit_message_prefix: "chore: "
